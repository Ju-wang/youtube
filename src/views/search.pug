extends base
include mixins/video

block content 
    form(method="GET") 
        input(type="text", placeholder="Search By Text", name="keyword", required)
        input(type="submit", value="Search Now")
    
    div
        each data in videos 
            +video(data)
    